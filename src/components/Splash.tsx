import { BrandIcon } from "../assets/icons";

export default function Splash() {
  return (
    <main className="fixed bottom-0 left-0 right-0 top-0 flex items-center justify-center bg-white">
      <BrandIcon size={60} color="#D000FF" />
    </main>
  );
}
